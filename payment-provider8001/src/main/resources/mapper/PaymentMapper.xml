<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycloud.payment.dao.PaymentDao">

    <resultMap id="BaseResultMap" type="com.mycloud.api.entities.Payment">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <id column="serial" jdbcType="VARCHAR" property="serial"/>
    </resultMap>

    <select id="getPaymentById" parameterType="Long"  resultMap="BaseResultMap">
        select *
        from payment where id = #{id};
    </select>

    <insert id="create" parameterType="Payment" useGeneratedKeys="true" keyProperty="id">
        insert into payment (serial)
        values (#{serial,jdbcType=VARCHAR})
    </insert>

</mapper>